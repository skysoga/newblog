import LgButtonGroup from '../button/src/button-group';

/* istanbul ignore next */
LgButtonGroup.install = function(Vue) {
  Vue.component(LgButtonGroup.name, LgButtonGroup);
};

export default LgButtonGroup;
